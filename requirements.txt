# Runtime + dev dependencies (mirrors pyproject.toml [project].dependencies).
# Install into a plain venv with:  pip install -r requirements.txt
# Run things as modules from the repo root: python -m src / python -m alembic / python -m pytest

# ── runtime ──
discord.py>=2.7
aiohttp>=3.9
sqlalchemy[asyncio]>=2.0
asyncpg>=0.29
alembic>=1.13
pydantic-settings>=2.0
jishaku>=2.7
anthropic>=0.69
wavelink>=3,<4
rich>=13
pynacl>=1.5
davey>=0.1.6

# ── dashboard (optional web UI under dashboard/) ──
fastapi>=0.110
uvicorn[standard]>=0.29
starlette>=0.37
httpx>=0.27
itsdangerous>=2.0       # required by starlette SessionMiddleware

# ── dev / tests ──
ruff
pytest
pytest-asyncio
aiosqlite
